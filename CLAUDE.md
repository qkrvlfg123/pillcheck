# CLAUDE.md — 필체크(PillCheck) 공통 규칙

이 파일은 Claude Code가 이 저장소에서 작업할 때 자동으로 읽는 컨텍스트입니다.
각 앱 폴더(`apps/*`)에는 자체 CLAUDE.md가 추가로 있습니다.

## 프로젝트 개요

고령자 대상 통합 헬스케어 플랫폼. 《요즘 바이브 코딩》 챕터 8·9·10 + 음성 비서를
하나의 모노레포로 통합. 모든 모듈은 `packages/shared`의 공용 데이터셋·엔진을 공유.

## 공통 원칙

- **이 플랫폼은 의료기기가 아니다.** 진단·처방을 하지 않으며, 모든 건강 관련
  출력은 "주의 안내"이고 최종 판단은 의사·약사가 한다. 이 문구는 사용자에게
  보이는 화면에 반드시 명시할 것.
- **고령자 접근성 우선.** 큰 글씨, 고대비, 쉬운 문장, 단계별 안내를 기본값으로.
- 실제 개인정보는 쓰지 않는다. 개발·데모는 가상·합성 데이터만 사용.

## 코드 스타일

- 파이썬: 표준 라이브러리 우선, 타입 힌트 권장, 함수는 작게.
- 프런트엔드: 각 앱 CLAUDE.md의 스택 규칙을 따른다.
- 커밋 메시지는 무엇을·왜 바꿨는지 한 줄로.

## 작업 워크플로우

1. **먼저 계획.** 코드를 바로 짜지 말고, 무엇을 만들지 계획을 먼저 세운다.
2. 새 기능은 새 git 브랜치에서 작업한다(안전망).
3. 변경 후에는 실행/테스트로 동작을 확인한다.

## 공용 엔진 사용법

약물-약물 상호작용 검사가 필요하면 `packages/shared/engine/drug_interaction.py`의
`check_interactions(drug_list)`를 사용한다. 새로 만들지 말 것.
